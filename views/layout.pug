<<<<<<< HEAD
doctype html
html
  head
    title= title
    script(src="https://code.jquery.com/jquery-2.2.4.js")
    link(rel="stylesheet" href='http://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css' media='screen')
    link(rel="stylesheet" href='/stylesheets/style.css')
    link(rel="stylesheet" href="https://fonts.googleapis.com/css?family=Inknut+Antiqua|Libre+Baskerville")
  body
    block link
      -var selected = 'Home'; //default

    -var menu = {'Home': '/','Account':'/account','Games':'/games','Characters':'/characters','Database':'/database'};

    nav(role='navigation').navbar.navbar-default.navbar-top-fixed
      .container-fluid
        .navbar-header
          a(href='/').navbar-brand Preparation
        ul.nav.navbar-nav
          each val, key in menu
            if (selected === key)
              li.active
                a(href=val, title=key)= key
            else
              li
                a(href=val, title=key)= key

    block content
=======
doctype html
html
  head
    title= title
    script(src="https://code.jquery.com/jquery-2.2.4.js")
    link(rel="stylesheet" href='http://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css' media='screen')
    link(rel="stylesheet" href='/stylesheets/style.css')
    link(rel="stylesheet" href="https://fonts.googleapis.com/css?family=Inknut+Antiqua|Libre+Baskerville")
  body
    block link
      -var selected = 'Home'; //default

    -var menu = {'Home': '/','Account':'/account','Games':'/games','Characters':'/characters','Database':'/database'};

    nav(role='navigation').navbar.navbar-default.navbar-top-fixed
      .container-fluid
        .navbar-header
          a(href='/').navbar-brand Preparation
        ul.nav.navbar-nav
          each val, key in menu
            if (selected === key)
              li.active
                a(href=val, title=key)= key
            else
              li
                a(href=val, title=key)= key

    block content
>>>>>>> cac6cef050b1a472a9382bdd6396773bc3af74e8
